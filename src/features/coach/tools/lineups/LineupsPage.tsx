import { PlaceholderPage } from '../../../../shared/layout/PlaceholderPage'

export function LineupsPage() {
  return (
    <PlaceholderPage
      kicker="Custom Tools · Lineups"
      title="Drag-and-drop boat builder."
      description="Phase 5 ports BoatCard / SeatCircle / RosterGrid from the rowiq-prototype, converts JSX→TSX, rethemes with synth tokens, and wires publish + lineup history into the schema."
      phase="Phase 5 · queued"
    />
  )
}
