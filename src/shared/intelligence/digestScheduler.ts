/**
 * Placeholder for Monday 6am digest (DATA_INTELLIGENCE §2.10).
 * Wire to Supabase cron or Edge Function when backend exists.
 */
export function describeDigestSchedule(): string {
  return 'Weekly digest: configure server cron → email coach (not running in static SPA).'
}
