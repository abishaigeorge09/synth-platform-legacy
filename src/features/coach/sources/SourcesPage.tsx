import { PlaceholderPage } from '../../../shared/layout/PlaceholderPage'

export function SourcesPage() {
  return (
    <PlaceholderPage
      kicker="Coach · Sources"
      title="Connected tools, sync status, logs."
      description="Phase 4 lists every connector with health badges, scan timestamps, and the markdown report viewer. The add-source flow opens via the synth. Agent modal portal (click the green Agent button in the sidebar)."
      phase="Phase 4 · queued"
    />
  )
}
