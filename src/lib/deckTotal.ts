/** Main pitch deck — must match slide count in `App.tsx` (primary deck). */
export const DECK_SLIDE_TOTAL = 17

/** Draft / appendix deck (`#appendix`) — `AppendixDeck.tsx`. */
export const APPENDIX_SLIDE_TOTAL = 7
