import { PlaceholderPage } from '../../../shared/layout/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      kicker="Coach · Settings"
      title="Team, integrations, visibility."
      description="Phase 9 surfaces team_settings.athlete_visibility_json as toggles, sync defaults (cron + stale thresholds), and per-user notification preferences."
      phase="Phase 9 · queued"
    />
  )
}
