import { PlaceholderPage } from '../../../../shared/layout/PlaceholderPage'

export function SessionTimerPage() {
  return (
    <PlaceholderPage
      kicker="Custom Tools · Session Timer"
      title="Strava-style piece timer."
      description="Phase 6 ports LiveSession / Stopwatch / TimerCard from the rowiq-prototype: requestAnimationFrame clock, multi-boat swipe, split recording, video chapter markers, and post-session report generation."
      phase="Phase 6 · queued"
    />
  )
}
